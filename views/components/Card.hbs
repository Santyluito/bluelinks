  <article class="card mb-2">

    <h5 class="card-header">{{item.shortURL}}</h5>

    <div class="card-body">
      <p>{{item.origin}}</p>
    </div>

    <div class="card-footer">
      <a href="#" class="btn btn-danger">Eliminar</a>
      <a href="#" class="btn btn-warning">Editar</a>
      <a href="#" class="btn btn-primary">Copiar</a>
    </div>

  </article>